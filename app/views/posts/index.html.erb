<h2><%= current_user ? current_user.blog_name : "ShotBlog" %></h2> 

<div><%= link_to 'New Post', new_post_path %> | <%= @show ? (link_to 'My Posts', posts_path) : (link_to 'All Posts', posts_path(show: 'all')) %></div>


<% @posts.each do |post| %>
	<div><%= link_to post.title, post %></div>
	<div><%#= image_tag post.image.url if post.image? %></div>
	<p>---------------------------------------------</p>
<% end %>


